# Bridge Private Keys - NEVER COMMIT REAL KEYS TO GIT!
# These are required for automated Step 3 transfers in the bridge

# Ethereum bank wallet private key (without 0x prefix)
ETHEREUM_BANK_PRIVATE_KEY=your_ethereum_private_key_here

# Solana bank wallet private key (base64 encoded)
SOLANA_BANK_PRIVATE_KEY=your_solana_private_key_base64_here

# XRPL bank wallet seed
XRPL_BANK_SEED=your_xrpl_seed_here

# RPC URLs (optional - defaults provided)
ETHEREUM_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/your_key
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Other required environment variables
DATABASE_URL=your_database_url
VITE_REOWN_PROJECT_ID=your_reown_project_id

# CRITICAL: VITE Environment Variables for Wallet Connections
# These MUST be set in Replit Secrets for production deployment to work

# WalletConnect Project ID (Required for wallet connections to work)
VITE_WALLETCONNECT_PROJECT_ID=fa5ebbd9b00cc684a0662cd09406ed00

# Fee Collection Wallet (Required for DEX swaps)
VITE_FEE_WALLET_EVM=0x9211346f428628d7C84CE1338C0b51FDdf2E8461

# 1inch API Key (Optional but improves swap functionality)
VITE_ONEINCH_API_KEY=your_1inch_api_key_here

# Wallet Connection Timeout Configuration
# Timeout for wallet connection QR codes in seconds (default: 600 = 10 minutes)
VITE_WALLET_CONNECTION_TIMEOUT=600
# Timeout for transaction confirmations in seconds (default: 300 = 5 minutes) 
VITE_TRANSACTION_TIMEOUT=300
# Timeout for session authentication in seconds (default: 480 = 8 minutes)
VITE_SESSION_TIMEOUT=480

# DEPLOYMENT INSTRUCTIONS:
# 1. In Replit, go to Secrets tab
# 2. Add each VITE_ variable above as a separate secret
# 3. Use the exact variable names (including VITE_ prefix)
# 4. These will be automatically available to your frontend when deployed